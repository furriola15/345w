<table class="table table-striped table-middle-aligned">
        <thead>
            <tr>
                <th scope="col">Nombre</th>
                <th scope="col">Apellido</th>
                <th scope="col">Email</th>
                <th scope="col">Telefono</th>
                <th scope="col">Opción</th>
            </tr>
        </thead>
        <tbody>
        {% for post in posts %}
            <tr id="tr_{{post.id}}">
                <td>{{ post.nombre }}</td>
                <td>{{ post.apellido }}</td>
                <td>{{ post.mail }}</td>
                <td>{{ post.telefono}}</td>
                <td class="text-right">
                    <div class="item-actions">
                        <a href="#" class="btn btn-sm btn-primary deletecita" data-id="{{post.id}}">
                            <i class="fa fa-times" aria-hidden="true"></i>
                        </a>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>