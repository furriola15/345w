{% extends 'base2.html.twig' %}

{% block styles %}
    <style>

    </style>
{% endblock %}

{% block main %}
    <div class="row">
        <div class="col-md-12 header">
            <h1 class="h3 mb-0 text-gray-800 float-left">INTERACTIVE MAP - {{project.nombre|upper}}
            </h1>

            <a class="btn btn-success btn-icon-split float-right" href="{{path('admin_new_doc')}}/{{project.id}}">
                <span class="icon">
                    <i class="fas fa-cog"></i>
                    Admin project
                </span>
            </a>
            {% if project.id == 2 %}
                <a class="btn btn-primary btn-icon-split float-right" href="{{path('tresw_dashboard_caribbean')}}/{{project.id}}/0">
                    <span class="icon">
                        <i class="fas fa-chart-bar"></i>
                        Dashboard
                    </span>
                </a>                   
            {% else %}
                <a class="btn btn-primary btn-icon-split float-right" href="{{path('tresw_dashboard')}}/{{project.id}}/0">
                <span class="icon">
                    <i class="fas fa-chart-bar"></i>
                    Dashboard
                </span>
            </a>
            {% endif %}
            <div class="clearfix"></div>
            <hr>
        </div>
        <div class="clearfix"></div><br>
<iframe style="width: 100%"  height="750" src="{{project.pbi}}" frameborder="0" allowFullScreen="true"></iframe>
        <div class="col-md-12">
 
    
            </div>
        </div>
        <div class='clearfix'></div>

        <div class='clearfix'></div><br>
    {% endblock %}
    {% block javascripts %}
        <script>
            $(function () {
               
            });
        </script>
    {% endblock %}
